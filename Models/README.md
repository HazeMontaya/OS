# Models
This directory is part of the portable OS root and is reserved for local model weights and bundled inference assets. Cloud/API models do not store secrets here. API keys must remain in environment variables or the future OS credential vault.
