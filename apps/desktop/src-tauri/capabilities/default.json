{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the primary OS window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
